export type ArObjectMode = 'bubble' | 'balloon'

/**
 * Switch between floating object types.
 * - `'balloon'` — rubber party balloons (current)
 * - `'bubble'`  — glass soap bubbles
 */
export const AR_OBJECT_MODE: ArObjectMode = 'balloon'
